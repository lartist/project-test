<form method="post" action="{{ path('call_for_projects_delete', {'id': call_for_project.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ call_for_project.id) }}">
    <button class="btn">Delete</button>
</form>
